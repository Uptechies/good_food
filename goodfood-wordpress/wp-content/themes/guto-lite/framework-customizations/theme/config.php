<?php if ( ! defined( 'FW' ) ) {
	wp_die(  'Forbidden' );
}

// phpcs:ignoreFile

/**
 * Theme config
 *
 * @var array $cfg Fill this array with configuration data
 */

$cfg['settings_form_side_tabs'] = true;